<!-- Describe what this PR changes in the release manifest or tooling. -->

## Summary

-

## Manifest changes (delete if none)

- [ ] New/updated `files` entries point to assets on a published `vX.Y.Z` release
- [ ] New/updated `patches` have correct `offset`/`size` and non-overlapping ranges
- [ ] `sha256` / `sha256_after` and `size` match the uploaded assets

## Checks

- [ ] `python3 scripts/validate_manifest.py` passes locally
- [ ] CI **validate** check is green
